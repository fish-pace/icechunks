# Python caches
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# Local Icechunk repository written by ocean-heat-test-local.ipynb. Metadata and
# virtual references only -- a few hundred KB -- but it is build output, not source.
coastwatch-ohc-http-icechunk-demo/

# Jupyter scratch notebooks
Untitled*.ipynb
Untitled*

# Credentials — NEVER commit Source Cooperative / STS tokens
*-creds.json
*creds*.json

# Claude Code machine-local settings. settings.local.json is per-person and per-machine
# (it accumulates permission rules naming /tmp paths from individual sessions). A shared
# .claude/settings.json, if we ever want one, is committed deliberately and not ignored.
.claude/settings.local.json

# OS / editor cruft
.DS_Store
*.swp
*.tmp
